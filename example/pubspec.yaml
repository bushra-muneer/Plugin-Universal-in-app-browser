name: universal_in_app_browser_example
description: Example app showcasing Plugin Universal In-App Browser.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_universal_in_app_browser:
    path: ../
  flutter_inappwebview: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
